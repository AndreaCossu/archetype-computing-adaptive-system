name: Build and Deploy Sphinx Docs

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install docs + project dependencies
        run: |
          python -m pip install --upgrade pip
          # If you have a requirements file, uncomment:
          # pip install -r requirements.txt
          pip install sphinx sphinx-rtd-theme
          # Install your package so autodoc imports work
          pip install -e .

      - name: Generate API docs (sphinx-apidoc)
        run: |
          sphinx-apidoc --force --no-toc -o docs/source acds
          sphinx-apidoc --force --no-toc -o docs/source experiments \
            experiments/adiac.py \
            experiments/adiac_dfa.py \
            experiments/adiac_trained.py \
            experiments/cifar10.py \
            experiments/cifar10_gray.py \
            experiments/libras.py \
            experiments/mackey_glass.py \
            experiments/mallat.py \
            experiments/psmnist.py \
            experiments/smnist.py \
            experiments/smnist_dfa.py \
            experiments/trace.py \
            experiments/trained_libras.py \
            experiments/trained_mackey_glass.py \
            experiments/trained_mallat.py \
            experiments/trained_smnist.py \
            experiments/trained_trace.py \
            experiments/equilibrium_propagation/main.py \
            experiments/sclbridge/rnn_avalanche.py
          sphinx-apidoc --force --no-toc -o docs/source robots_demo

      - name: Build Sphinx HTML
        run: |
          sphinx-build -b html docs/source docs/_build/html

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/_build/html

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
